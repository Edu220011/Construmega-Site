# Produção - URL completa da API na VPS
# Opção 1: Com domínio e HTTPS (recomendado após configurar Nginx + SSL)
REACT_APP_API_URL=https://construmega.com.br

# Opção 2: Com domínio e HTTP (temporário, enquanto não tiver SSL)
# REACT_APP_API_URL=http://construmega.com.br

# Opção 3: Direto pelo IP (funcionará, mas não recomendado para produção)
# REACT_APP_API_URL=http://129.121.35.197

# IMPORTANTE: Após configurar Nginx com SSL, usar HTTPS
# Para build local de teste: npm run build
# O Nginx irá fazer proxy reverso e expor tudo na porta 80/443
