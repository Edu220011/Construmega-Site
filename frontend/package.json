{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@mercadopago/sdk-react": "^1.0.7",
    "buffer": "^6.0.3",
    "path-browserify": "^1.0.1",
    "process": "^0.11.10",
    "quagga": "^0.12.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.2",
    "stream-browserify": "^3.0.0"
  },
  "scripts": {
    "start": "export NODE_OPTIONS=--no-deprecation && react-app-rewired start",
    "build": "export NODE_OPTIONS=--no-deprecation && react-app-rewired build",
    "test": "export NODE_OPTIONS=--no-deprecation && react-app-rewired test",
    "eject": "react-scripts eject",
    "serve": "serve -s build -l 8080"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "react-app-rewired": "^2.2.1",
    "react-scripts": "^5.0.1"
  }
}
